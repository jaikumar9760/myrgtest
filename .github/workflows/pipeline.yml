name: "Ankit Kumar GitHub"
on:
  push:
    branches:
      - 'feature/**'
      - 'main'
      - 'hotfix/**' 

jobs:
  terraformInit:
    name: Terraform Init
    runs-on: [self-hosted, Windows, X64]  # Corrected line
    steps:
    - name: First Step
      run: echo "Hello World from Ankit-Kumar Runner and This job is running on: $RUNNER_NAME"
