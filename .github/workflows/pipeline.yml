name: "Ankit Kumar GitHub"
on:
  push:
    branches:
      - 'feature/**'
      - 'main'
      - 'hotfix/**' 

jobs:
  terraformInit:
    name: Terraform Init
    runs-on: Ankit-Kumar
    steps:
    - name: First Step
      run: echo "Hello World from Ankit-Kumar Runner"
